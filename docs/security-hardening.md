# Security Hardening

Recommended baseline:
- Pin dependencies in CI where possible
- Run andit and pip-audit
- Keep secrets out of repo
- Use GitHub branch protection + CODEOWNERS
- Periodically purge stale CI caches
